{
  "name": "oj-sample",
  "private": true,
  "type": "module",
  "scripts": {
    "unit:install": "npm --prefix unit ci",
    "typecheck": "npm --prefix unit run typecheck",
    "test:unit": "npm --prefix unit run test",
    "test:api": "cd .. && cargo run --release -p xtask -- build && ./bin/oj test -c sample/config.yaml -d sample/src",
    "test": "npm run typecheck && npm run test:unit && npm run test:api"
  },
  "dependencies": {
    "escape-goat": "4.0.0"
  }
}
