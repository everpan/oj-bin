name: "oidc"
desc: "RP（OIDC Relying Party）：login/callback/logout，通用 discovery 对接任意标准 IdP，会话桥接复用 auth/_shared"
version: "0.1.0"
deps:
  _platform: "^0.1.0"
  # callback 用 `#/auth/_shared/…` 别名跨模块引用 → S008 要求声明
  # （login/logout 仍是相对写法，不追溯）
  auth: "^0.1.0"
